using System;
using System.Collections.Generic;
using System.Text;
namespace SI_PDF1
{
    class Q2
    {
        //static void Main(string[] args)
        //{
        //    string s = "hello world";
        //    s.Replace('o', 'i');
        //    s.Replace("r", string.Empty);
        //    Console.WriteLine("Expected : hello world & actual : " + s);
        //    Console.WriteLine("Reason: Even though we are replacing characters we're not assigning the replaced string to the main string hence the main string stays as it is if we would have assigned the replaced string back to the main string the output would have been: helli wild");
        //}
    }
}
